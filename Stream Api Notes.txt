1.Consumer
	-consumer interface is used to perform some operations on array list and produce result as per our requirement
eg-
public class ConsumerExaple{

main(){
Consumer<String> consumer = (s)->System.out::println(s.toUpperCase);
s.accept("pradip");
}

}

2.Consumer Chain

Consumer<Student> c1 = (student)->Syetem.out::println(student.getName());
Consumer<Student> c2 = (student)->Syetem.out::println(student.getActivities());

List<Student> students = studentsData.getall();

for(Student:students){
	c1.andThen(c2).accept(student);

}
